The Mean Square Error of an estimator $\hat{\theta}$ of parameter $\theta$ is defined by

$$\large
\text{MSE}(\hat{\theta}) = E[(\hat{\theta}-\theta)^{2]}= \text{var}(\hat{\theta}) + [\text{Bias}_n(\hat{\theta})]^2
$$
